<h1>Log In</h1>

<%= form_with model: @user, url: "/sessions" do |f| %>
  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.label :password %>
  <%= f.password_field :password %>

  <%= f.submit "Log In" %>
<% end %>

<% flash.each do |key, msg| %>
  <p><%= msg %></p>
<% end %>